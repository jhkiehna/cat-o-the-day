import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 0.5,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'space-around',
  },
  catimg: {
    width: '80%',
    height: '50%',
    resizeMode: 'contain',
  },
});
