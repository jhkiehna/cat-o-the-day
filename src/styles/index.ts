import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 30,
    backgroundColor: '#fff',
    alignItems: 'center',
  },
  header: {
    fontSize: 20,
  },
  catimg: {
    marginTop: 20,
    flex: 1,
    width: '80%',
    height: 200,
  },
});
